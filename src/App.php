<?php

namespace AKlump\ChangeAudio;

class App {

  /**
   * The executable name; the config and cache directories are named after it.
   */
  const BIN = 'chaudio';

  const NAME = 'chaudio';

  /**
   * Keep in step with .web_package/config.yml when releasing.
   */
  const VERSION = '0.0.17';
}
